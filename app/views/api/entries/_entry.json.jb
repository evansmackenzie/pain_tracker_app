{
  id: entry.id,
  variable_id: entry.variable_id,
  value: entry.value,
  rating: entry.rating,
  created_at: entry.created_at.strftime("%m/%d/%Y"),
  updated_at: entry.updated_at.strftime("%m/%d/%Y")
}